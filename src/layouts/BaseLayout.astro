---
import { site } from "../../site.config.mjs";
import "../styles/global.css";

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  ogType?: string;
  alternates?: Array<{ hreflang: string; href: string }>;
}
const { title, description = site.description, ogImage, ogType = "website", alternates } = Astro.props;
const resolvedOgImage = ogImage ?? `${site.url}/favicon.png`;

const fams = [site.brand.fontDisplay, site.brand.fontBody]
  .map((f) => f.replace(/ /g, "+") + ":wght@400;600;700")
  .join("&family=");
const fontsHref = `https://fonts.googleapis.com/css2?family=${fams}&display=swap`;

const brandVars = `
  --bg:${site.brand.bg};
  --surface:${site.brand.surface};
  --text:${site.brand.text};
  --muted:${site.brand.muted};
  --accent:${site.brand.accent};
  --accent-text:${site.brand.accentText};
  --font-display:"${site.brand.fontDisplay}",serif;
  --font-body:"${site.brand.fontBody}",system-ui,sans-serif;
`;
---

<!doctype html>
<html lang={site.lang}>
  <head>
    <!-- Google tag (gtag.js) -->
    <script is:inline async src="https://www.googletagmanager.com/gtag/js?id=AW-17977352986"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'AW-17977352986');
    </script>
    <!-- Microsoft Clarity -->
    <script type="text/javascript" is:inline>
      (function(c,l,a,r,i,t,y){
          c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
          t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
          y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
      })(window, document, "clarity", "script", "xr1fwqa9cs");
    </script>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="msvalidate.01" content="7BC48F9D0740327D02EED2218E21311A" />
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={new URL(Astro.url.pathname, site.url)} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="stylesheet" href={fontsHref} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content={ogType} />
    <meta property="og:url" content={new URL(Astro.url.pathname, site.url)} />
    <meta property="og:image" content={resolvedOgImage} />
    <meta property="og:locale" content="es_MX" />
    <meta property="og:site_name" content={site.name} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={resolvedOgImage} />
    {alternates?.map(alt => (
      <link rel="alternate" hreflang={alt.hreflang} href={alt.href} />
    ))}
    <link rel="sitemap" type="application/xml" href="/sitemap.xml" />
    <style set:html={`:root{${brandVars}}`}></style>
    <script type="application/ld+json">{
      "@context": "https://schema.org",
      "@type": "Organization",
      "@id": "https://www.jarmar.com/#organization",
      "name": "Ópticas Jarmar",
      "alternateName": "Clínica Óptica Boutique Jarmar",
      "url": "https://www.jarmar.com/",
      "logo": "https://www.jarmar.com/logo-opticas-jarmar-color.png",
      "foundingDate": "1966",
      "description": "Clínica Óptica Boutique con más de 60 años cuidando la salud visual de México. Especialistas en lentes esclerales, con sucursales en Tuxtla Gutiérrez, Chiapas y Tijuana, Baja California.",
      "sameAs": [
        "https://www.facebook.com/opticasjarmarchiapas",
        "https://www.instagram.com/opticasjarmar/",
        "https://www.facebook.com/opticasjarmartijuana",
        "https://www.instagram.com/opticasjarmartijuana/"
      ],
      "aggregateRating": {
        "@type": "AggregateRating",
        "ratingValue": "4.9",
        "reviewCount": "880",
        "bestRating": "5"
      }
    }</script>
    <slot name="head" />
  </head>
  <body>

    <!-- ═══ NAVBAR ═══ -->
    <nav class="site-nav" id="nav">
      <div class="nav-container">
        <a href="/" class="nav-logo">
          <img src="/logo-opticas-jarmar-blanco-esp.png" alt="Ópticas Jarmar" />
        </a>
        <div class="nav-links" id="navLinks">
          <button class="nav-close" id="navClose" aria-label="Cerrar menú">✕</button>
          <a href="/">Inicio</a>
          <a href="/chiapas/">Chiapas</a>
          <a href="/tijuana/">Tijuana</a>
          <a href="/historia/">Historia</a>
          <a href="/blog/">Blog</a>
          <a href="https://wa.me/529611855475" target="_blank" class="nav-cta">Agendar cita</a>
        </div>
        <button class="nav-burger" id="burger" aria-label="Menú">
          <span></span><span></span><span></span>
        </button>
      </div>
    </nav>

    <main>
      <slot />
    </main>

    <!-- ═══ FOOTER ═══ -->
    <footer class="site-footer">
      <div class="footer-container">
        <div class="footer-brand">
          <img src="/logo-opticas-jarmar-blanco-esp.png" alt="Ópticas Jarmar" style="height:36px; width:auto; opacity:0.85;" />
          <p>{site.description}</p>
        </div>
        <div class="footer-links">
          <a href="/chiapas/">Chiapas</a>
          <a href="/tijuana/">Tijuana</a>
          <a href="/historia/">Historia</a>
          <a href="/blog/">Blog</a>
          <a href="/sitemap.xml">Sitemap</a>
        </div>
        <p class="footer-copy">© {new Date().getFullYear()} {site.org.legalName} · Todos los derechos reservados</p>
      </div>
    </footer>

    <script>
      const nav = document.getElementById('nav');
      window.addEventListener('scroll', () => nav.classList.toggle('scrolled', window.scrollY > 60));
      nav.classList.add('scrolled');
      const navLinks = document.getElementById('navLinks');
      document.getElementById('burger').addEventListener('click', () => navLinks.classList.toggle('open'));
      document.getElementById('navClose').addEventListener('click', () => navLinks.classList.remove('open'));
      document.querySelectorAll('#navLinks a').forEach(a => a.addEventListener('click', () => navLinks.classList.remove('open')));

      // Google Ads conversion: fire on every WhatsApp CTA click
      document.querySelectorAll('a[href*="wa.me"]').forEach(function(el) {
        el.addEventListener('click', function() {
          if (typeof gtag === 'function') {
            gtag('event', 'conversion', {
              send_to: 'AW-17977352986/zRKPCO6GotIcEJrGovxC',
              value: 1.0,
              currency: 'MXN'
            });
          }
        });
      });
    </script>

  </body>
</html>
